import { useState } from "react";
import { motion } from "framer-motion";
import { useNavigate } from "react-router-dom";

const ModeSelector = () => {
  return null; // Mode selection is now handled by separate pages
};

export default ModeSelector;
